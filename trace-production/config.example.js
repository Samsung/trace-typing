/**
 * Example configuration file.
 * Create a config.js file in the same directory as this file, and adjust paths appropriately.
 *
 * See config.default.js for options.
 */
var config = {
    tracesDirectory: "/Users/e.andreasen/tmp/traces"
};

exports.config = config;